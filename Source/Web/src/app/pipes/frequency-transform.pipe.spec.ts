import { FrequencyTransformPipe } from './frequency-transform.pipe';

describe('FrequencyTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new FrequencyTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
